import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Thay đổi base thành tên repository GitHub của bạn.
  // Ví dụ: Nếu link github là github.com/nekkozumi/TheThing thì base: '/TheThing/'
  // Nếu bạn dùng custom domain hoặc dạng nekkozumi.github.io thì base: '/'
  base: '/TheThing/',
});
